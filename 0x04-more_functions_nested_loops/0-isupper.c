#include "main.h"

/**
 * _isupper - Checks for uppercase character.
 * @c: Decimal equivalent of the character to be checked
 * Return: 1 if c is upper 0 otherwise
 */
int _isupper(int c)
{
	return (1 * (c >= 65 && c <= 90));
}

#ifndef MAIN_H
#define MAIN_H

/* headerfiles */
#include <stdio.h>
#include <unistd.h>
#include <string.h>

/* declarations */
void _putchar(int);
char *_strcat(char *dest, char *src);
char *_strncat(char *dest, char *src, int n);

#endif

#include "main.h"

/**
 * wildcmp - Compares two strings
 * Description: s2 can contain special character * and it can replace
 * any character including empty string
 * @s1: First string
 * @s2: Second string (pattern)
 * Return: 1 if the strings can be considered identical otherwise 0
 */
int wildcmp(char *s1, char *s2)
{
	/* TODO */
}
